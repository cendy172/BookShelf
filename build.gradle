apply plugin: 'idea'
apply plugin: 'java'

version = '1.0'

repositories {
    mavenCentral()
}

dependencies {
    compile(
            project(':core'),
            project(':web')
    )
    testCompile(
            'junit:junit:4.11'
    )
}